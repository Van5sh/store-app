import { registerRootComponent } from 'expo';
import App from './App';

// This ensures the environment is set up appropriately whether you load the app in Expo Go or a native build.
registerRootComponent(App);